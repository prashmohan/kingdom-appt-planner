version: '2.4'
services:
  web:
    build: .
    ports:
      - "12348:5000"
    volumes:
      - planner_data:/app/data
    environment:
      - DATABASE_PATH=/app/data/planner.db
volumes:
  planner_data:
